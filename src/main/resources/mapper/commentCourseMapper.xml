<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bit.data.dao.CommentCourseDao">
    <select id="selectCafeStar" parameterType="int" resultType="ccdto">
        select * from comment_course where cafe_num=#{cafe_num}
    </select>
    <insert id="insertCafeStar" parameterType="Map">
        insert into comment_course values (null,#{user_num},null,null,#{cafe_num},#{star},#{content},now())
    </insert>

</mapper>